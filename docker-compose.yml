services:
  battle-ants:
    # After the GitHub Action has run at least once, you can swap this for the published image:
    #   image: ghcr.io/<your-github-username>/<your-repo-name>:latest
    build: .
    container_name: battle-ants
    restart: unless-stopped
    ports:
      - "8080:80"
